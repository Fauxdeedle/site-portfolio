@AGENTS.md

Project content for `/work/[slug]` pages lives in `content/projects/` as
markdown files with YAML frontmatter, loaded by `lib/projects.js` — see
`content/projects/CLAUDE.md` for how that system works.
